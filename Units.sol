pragma solidity ^0.4.0;

contract SimpleContract{
   //ether szabo finney wei 
   bool isEther = (1 ether == 4000 finney);
   
   //time units
   bool isTime(1 = 1 seconds);
   
   //seconds, hours, days, weeks
   bool isTime(1 hours == 60 minutes);
   
}
